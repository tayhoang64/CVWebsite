@model WebCV.Models.Slider

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Slider</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit" enctype="multipart/form-data" id="template-form">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="SlideID" />
            <div class="form-group">
                <label asp-for="Title" class="control-label"></label>
                <input asp-for="Title" class="form-control" />
                <span asp-validation-for="Title" id="title-error" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Image" class="control-label"></label>
                <input name="img" asp-for="Image" class="form-control" type="file"/>
                <span asp-validation-for="Image" id="img-error" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Description" class="control-label"></label>
                <input asp-for="Description" class="form-control" />
                <span asp-validation-for="Description" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Order" class="control-label"></label>
                <input asp-for="Order" class="form-control" type="number"/>
                <span asp-validation-for="Order" id="order-error" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Link" class="control-label"></label>
                <input asp-for="Link" class="form-control" />
                <span asp-validation-for="Link" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
<!--
<script>
    document.getElementById('template-form').onsubmit = (e) => {
        e.preventDefault();
        let check = true;
        var form = document.getElementById('template-form');
        var formObject = new FormData(form);
        let title = formObject.get("title");
        let description = formObject.get("Description");
        let order = formObject.get("Order");
        let link = formObject.get("link");
        let image = formObject.get("image");

        if (title.length === 0) {
            document.getElementById('title-error').innerHTML = "Title is required";
            check = false;
        } else {
            document.getElementById('title-error').innerHTML = "";
        }

        var english = /^[A-Za-z0-9-]*$/;
        if (link.length === 0) {
            document.getElementById('link-error').innerHTML = "Link is required";
            check = false;
        } else if (!english.test(link)) {
            document.getElementById('link-error').innerHTML = "Link must be alphanumeric";
            check = false;
        } else {
            document.getElementById('link-error').innerHTML = "";
        }
        if (order.length <= 0) {
            document.getElementById('order-error').innerHTML = "Order must be higher than zero";
            check = false;
        } else {
            document.getElementById('order-error').innerHTML = "";
        }

        if (check) {
            form.submit();
        }
    }
</script>
-->